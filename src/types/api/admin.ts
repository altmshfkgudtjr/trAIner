import type { BaseQuery, BaseMutation } from 'types/api';

/* ================================================== */

export type CTR_threshold_query = BaseQuery<{}, number>;

/* ================================================== */

export type CTR_threshold_mutation = BaseMutation<
  {
    value: number;
  },
  {}
>;

/* ================================================== */

export type deep_threshold_query = BaseQuery<{}, number>;

/* ================================================== */

export type deep_threshold_mutation = BaseMutation<
  {
    value: number;
  },
  {}
>;

/* ================================================== */

export type hotuser_random_query = BaseQuery<{}, number>;

/* ================================================== */

export type hotuser_random_mutation = BaseMutation<
  {
    value: number;
  },
  {}
>;

/* ================================================== */

export type topic_similarity_query = BaseQuery<{}, number>;

/* ================================================== */

export type topic_similarity_mutation = BaseMutation<
  {
    value: number;
  },
  {}
>;

/* ================================================== */

export type cold_to_hot_query = BaseQuery<{}, number>;

/* ================================================== */

export type cold_to_hot_mutation = BaseMutation<
  {
    value: number;
  },
  {}
>;

/* ================================================== */
/* ================================================== */
/* ================================================== */
/* ================================================== */

/* ================================================== */
/* ================================================== */
/* ================================================== */
/* ================================================== */
