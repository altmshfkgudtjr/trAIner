// styels
import lightTheme from 'tds/lib/palette/light';
import darkTheme from 'tds/lib/palette/dark';
// types
import type { ThemePalette } from 'tds/types/Palette';

/** 테마 색상표 SET */
const themePalette: ThemePalette = {
  light: lightTheme,
  dark: darkTheme,
};

export default themePalette;
